generator-routeinjector
=======================

Install the generator globally:
```sh
$ npm -g install yo GerardSoleCa/generator-routeinjector
```

Scaffold a new application:
```sh
$ mkdir appName
$ yo routeinjector
``` 

Create a new model:
```sh
$ yo routeinjector:model
``` 
